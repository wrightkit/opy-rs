# OPY HIR contracts

The current wire contract is [Opy HIR v2](opy-hir-v2.md).
It retains the [Opy HIR v1](opy-hir-v1.md) payload and validation baseline
except where v2 explicitly changes it.

Read v2 first for current producer/consumer behavior. Consult v1 only for the
retained baseline or migration compatibility. Neither document overrides the
current source implementation and executable protocol tests.
