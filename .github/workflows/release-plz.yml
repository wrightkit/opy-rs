name: Release-plz

on:
  push:
    branches: [main]

jobs:
  release-plz:
    uses: wrightkit/.github/.github/workflows/release-plz-crates.yml@cbeaee0fda39369e288de4b2fb9078d3f10847c9
    permissions:
      contents: write
      pull-requests: write
    secrets:
      github-token: ${{ secrets.GH_TOKEN }}
      cargo-registry-token: ${{ secrets.CARGO_REGISTRY_TOKEN }}
