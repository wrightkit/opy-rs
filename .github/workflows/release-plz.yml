name: Release-plz

on:
  push:
    branches: [main]

jobs:
  release-plz:
    uses: wrightkit/.github/.github/workflows/release-plz-crates.yml@main
    permissions:
      contents: write
      pull-requests: write
    secrets:
      github-token: ${{ secrets.GH_TOKEN }}
      cargo-registry-token: ${{ secrets.CARGO_REGISTRY_TOKEN }}

  provider:
    name: Provider (${{ matrix.target }})
    if: needs.release-plz.outputs.releases_created == 'true'
    needs: release-plz
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            target: x86_64-unknown-linux-gnu
          - os: macos-15-intel
            target: x86_64-apple-darwin
          - os: macos-14
            target: aarch64-apple-darwin
          - os: windows-latest
            target: x86_64-pc-windows-msvc

    steps:
      - name: Check out release source
        uses: actions/checkout@v7
        with:
          ref: ${{ fromJSON(needs.release-plz.outputs.releases)[0].tag }}

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@master
        with:
          toolchain: stable
          targets: ${{ matrix.target }}

      - name: Build provider
        run: cargo build --locked --release --target ${{ matrix.target }} -p opy-provider --bin opy-provider

      - name: Archive provider
        shell: bash
        env:
          TARGET: ${{ matrix.target }}
          VERSION: ${{ fromJSON(needs.release-plz.outputs.releases)[0].tag }}
        run: |
          VERSION="${VERSION#v}"
          mkdir -p dist
          if [[ "$TARGET" == *windows* ]]; then
            BINARY=opy-provider.exe
          else
            BINARY=opy-provider
          fi
          cp "target/$TARGET/release/$BINARY" "dist/$BINARY"
          tar -czf "opy-provider-$VERSION-$TARGET.tar.gz" -C dist "$BINARY"
          ARCHIVE="opy-provider-$VERSION-$TARGET.tar.gz"
          CHECKSUM="${ARCHIVE}.sha256"
          if command -v sha256sum >/dev/null 2>&1; then
            sha256sum "$ARCHIVE" > "$CHECKSUM"
          else
            shasum -a 256 "$ARCHIVE" > "$CHECKSUM"
          fi

      - name: Stage provider artifact and checksum
        uses: actions/upload-artifact@v4
        with:
          name: opy-provider-${{ matrix.target }}
          path: |
            opy-provider-*.tar.gz
            opy-provider-*.tar.gz.sha256

  publish-provider:
    name: Publish provider release artifacts
    if: needs.release-plz.outputs.releases_created == 'true'
    needs:
      - release-plz
      - provider
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Download all target artifacts
        uses: actions/download-artifact@v4
        with:
          path: dist

      - name: Upload provider artifacts to the GitHub Release
        env:
          GH_TOKEN: ${{ github.token }}
          RELEASE_TAG: ${{ fromJSON(needs.release-plz.outputs.releases)[0].tag }}
        run: |
          set -euo pipefail
          gh release upload "$RELEASE_TAG" \
            --repo "$GITHUB_REPOSITORY" \
            dist/*/opy-provider-*.tar.gz \
            dist/*/opy-provider-*.tar.gz.sha256 \
            --clobber
